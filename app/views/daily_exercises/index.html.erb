

<table>
  <tr>
    <th>Date</th>
    <th>Exercises</th>
    <th>Reps</th>
    <th>Weight (lbs)</th>
    <th></th>
  </tr>

<% @daily_exercises.each do |daily_exercise| %>
  <tr>
    <td><%= daily_exercise.date %></td>
    <td><%= daily_exercise.exercises %></td>
    <td><%= daily_exercise.reps %></td>
    <td><%= daily_exercise.weight %></td>
    <td><%= link_to 'Show', daily_exercise %></td>
    <td><%= link_to 'Edit', edit_daily_exercise_path(daily_exercise) %></td>
    <td><%= link_to 'Destroy', daily_exercise, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<p><a class="btn btn-primary btn-large" <%= link_to 'New Daily exercise', new_daily_exercise_path %></a></p>

<table>
  <tr>
    <th>Date</th>
    <th>Exercises</th>
    <th>Reps</th>
    <th>Weight (lbs)</th>
    <th></th>
  </tr>

<% @daily_exercises.each do |daily_exercise| %>
  <tr>
    <td><%= daily_exercise.date %></td>
    <td><%= daily_exercise.exercises %></td>
    <td><%= daily_exercise.reps %></td>
    <td><%= daily_exercise.weight %></td>
    <td><%= link_to 'Show', daily_exercise %></td>
    <td><%= link_to 'Edit', edit_daily_exercise_path(daily_exercise) %></td>
    <td><%= link_to 'Destroy', daily_exercise, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<!--<p><a class="btn btn-primary btn-large" <%= link_to 'New Daily exercise', new_daily_exercise_path %>>New Daily exercise</a></p>-->

